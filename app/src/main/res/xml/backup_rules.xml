<?xml version="1.0" encoding="utf-8"?><!--
   Sample backup rules file; uncomment and customize as necessary.
   See https://developer.android.com/guide/topics/data/autobackup
   for details.
   Note: This file is ignored for devices older than API 31
   See https://developer.android.com/about/versions/12/backup-restore
-->
<full-backup-content>
    <!-- Never back up translation-provider secrets/endpoints. The keys are AES-GCM encrypted with a
         device-bound Android Keystore key, so a restored copy would be undecryptable anyway. -->
    <exclude domain="sharedpref" path="translator_keys_enc.xml"/>
    <exclude domain="sharedpref" path="translator_keys.xml"/>
    <exclude domain="sharedpref" path="translator_endpoints.xml"/>
    <!-- Premium entitlement is a local cache; the source of truth is Play Billing. Excluding it
         stops a backup/restore from carrying over a stale or forced premium flag. -->
    <exclude domain="sharedpref" path="entitlement.xml"/>
    <!-- Premium purchase cache written by BillingManager (prefs name "billing"); source of truth is
         Play Billing. Excluding it stops a backup/restore from carrying a stale/forced premium flag
         to another device before the next Play sync. -->
    <exclude domain="sharedpref" path="billing.xml"/>
</full-backup-content>